name: default
budget: 30
seed: 0
features: all          # or a list of feature names, or {feature: weight}
strategies:
  # Uncomment once the dialogue group is extracted.
  # - kind: target
  #   label: corrected
  #   quota: 0.3
  #   where: "user_corrects == 1"
  #   within: diversity
  - kind: outlier
    label: outlier
    quota: 0.3
    method: isolation_forest
  - kind: diversity
    label: diverse
    quota: rest
